{
    "type": "postgres",
    "host": "localhost",
    "port": 5432,
    "username": "the",
    "password": "cafe",
    "database": "theodercafe",
    "synchronize": false,
    "logging": true,
    "entities": ["src/**/*.entity.ts", "src/*.entity.ts", "dist/**/*.entity.js"],
    "migrations": ["migration/*.ts", "dist/db/migrations/**/*.js"],
    "cli": {
        "entitiesDir": "src",
        "migrationsDir": "migration",
        "subscribersDir": "subscriber"
    }
}
